# AWS Secrets Manager Example

This example shows the bundled `awsSecret` loader in a normal Go application.

## Run

```bash
BREK_CONFIG_DIR=examples/awssecret/config go run ./examples/awssecret
```

The example expects AWS credentials and access to the `demo` secret in `us-west-2`.

